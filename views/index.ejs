<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Todo List</title>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,800;1,200;1,600;1,700;1,800&display=swap" rel="stylesheet">



    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
 <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <div class="container ">
        <form action="/addTodo" method="POST">
            <h1 >Todo List </h1>
            <div class="formBody">
                <input type="text" placeholder="Thing To Do" name="task">
                <button>ADD</button>

        </div>
            </form>
            <hr/>

     <div class="tasks"> 
            <% for(let i=0; i < items.length; i++) { %>

        <div class="task"> 
                        <div>

                    <input type=checkbox  name="check" id="checkId" value="<%= LeftTodo%>"/>
                    <span>
                            <%= items[i].task %>

                            </span>
                        </div>
                    <!--<% if(items[i].completed === true ) {%>-->
                        <!--<span class='completed'><%= items[i].task %> </span> -->
                    <!--<%} else {%>-->
                    <!--<% } %>-->
        
                    <div>
                        <i class='fa fa-trash'></i></div>
                    </div>
        <% } %>

    <h2>Left to do:<%= LeftTodo %> </h2>
</div>
 </div>
    <script src='js/main.js'></script>
</body>
</html>
